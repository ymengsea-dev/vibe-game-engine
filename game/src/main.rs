//! Example game project for the Vibe Game Engine (VGE).
//!
//! This binary is the engine's first consumer: each milestone extends it
//! to exercise newly implemented engine features.

#[expect(
    clippy::print_stdout,
    reason = "placeholder until Milestone 1 wires up engine startup and logging"
)]
fn main() {
    // Milestone 1 replaces this with engine startup and a rendering window.
    println!("Vibe Game Engine (VGE) — engine runtime not yet implemented.");
}
